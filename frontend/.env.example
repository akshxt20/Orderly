# Base URL of the backend API — must include the /api/v1 suffix.
# Local dev points at the docker-compose backend.
# In Netlify, set this to your Render URL, e.g. https://orderly-api.onrender.com/api/v1
VITE_API_URL=http://localhost:8000/api/v1
